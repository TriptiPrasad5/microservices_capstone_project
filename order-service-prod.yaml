spring:
 datasource:
   url: ${DB_URL:jdbc:mysql://localhost:3306/orderdb_prod?createDatabaseIfNotExist=true}
   username: ${DB_USERNAME}
   password: ${DB_PASSWORD}
 jpa:
   show-sql: false
server:
 port: 7000
logging:
 level:
   root: ERROR
