spring:
 datasource:
   url: ${DB_URL:jdbc:mysql://localhost:3306/customerdb_prod?createDatabaseIfNotExist=true}
   username: ${DB_USERNAME}
   password: ${DB_PASSWORD}
 jpa:
   show-sql: false
server:
 port: 7001
logging:
 level:
   root: ERROR
