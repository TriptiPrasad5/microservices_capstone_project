spring:
 datasource:
   url: ${DB_URL:jdbc:mysql://localhost:3306/productdb_prod?createDatabaseIfNotExist=true}
   username: ${DB_USERNAME}
   password: ${DB_PASSWORD}
 jpa:
   show-sql: false
server:
 port: 7002
logging:
 level:
   root: ERROR
