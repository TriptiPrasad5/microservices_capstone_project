spring:
 datasource:
   url: ${DB_URL:jdbc:mysql://localhost:3306/orderdb_dev?createDatabaseIfNotExist=true}
   username: ${DB_USERNAME}
   password: ${DB_PASSWORD}
 jpa:
   show-sql: true
server:
 port: 7000
logging:
 level:
   root: DEBUG
